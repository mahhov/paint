{
  "name": "paint",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "main": "src/script.ts",
  "scripts": {
    "copy": "cp src/index.html out/index.html && cp media/favicon.png out/favicon.png",
    "build": "mkdir out && npm run copy && tsc",
    "dev": "npm run copy && tsc -w"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
